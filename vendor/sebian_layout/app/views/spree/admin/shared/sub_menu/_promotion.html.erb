<ul id="sidebar-promotions" class="collapse nav nav-pills nav-stacked <%= (cookies[:expanded_menu].present? && cookies[:expanded_menu].split('|').include?('#sidebar-promotions')) ? 'in' : '' %>" data-hook="admin_promotion_sub_tabs">
  <%= tab :promotions %>
  <%= tab :promotion_categories %>
</ul>
