<ul id="sidebar-product" class="collapse nav nav-pills nav-stacked <%= (cookies[:expanded_menu].present? && cookies[:expanded_menu].split('|').include?('#sidebar-product')) ? 'in' : '' %>" data-hook="admin_product_sub_tabs">
  <%= tab *Spree::BackendConfiguration::PRODUCT_TABS %>
  <%= tab :option_types, :match_path => '/option_types' %>
  <%= tab :properties %>
  <%= tab :prototypes %>
  <%= tab :taxonomies %>
  <%= tab :taxons %>
</ul>
